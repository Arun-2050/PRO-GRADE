# all the learnings of backend javascript
npm init
first make folders called public temp under that git.keep
make src folder make folder like models controllers,routers,middlewares,db,utils
 then install nodemon as web dev --save-dev
 git ignore file making + .env file go to gitignore gerators and copy the code of node environment paste in gitignore also copy .env file init
 install prettier then make .pretterrc and .prettierignore and do settings of prettier + ignore the files like .env *.env nodemodules /.vsocde
 dev:"nodemon src/index.js" in src make file like app.js contants.js index.js
type:moudle in package.json
# database connection 
make contants of the youtube
db.js mei build logic using moongoose and contants variable 
import dotenv file in index.js give path using path:.env
also make sure the to export the fn form db to index using export and import
and run the fn and npm run dev
# things to remember
use things are of middleware and cookie parser for project
import app thing and use listen on and other express funtion
# .env
accesstoken
expirytoken
refresh token
